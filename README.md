# DailyX
## Version v1.1

This release rebrands the app from **Daily Routine** to **DailyX**.

### v1.1 changes
- App name changed to DailyX
- PWA name and short name changed to DailyX
- New DailyX clock + checkmark app icon
- Version incremented from v1.0 to v1.1
- Existing `routineV3` localStorage data is preserved
- Existing routines, recurring tasks, copy tasks, deadlines, progress, streaks and date selection remain
- Footer: Made by AyushXO 😅

Upload/replace the files in the repository root:
`index.html`, `manifest.json`, `sw.js`, `icon.svg`, `icon-192.png`, `icon-512.png`, `README.md`.

If the old home-screen name/icon remains, reinstall the PWA after the new files are live.
